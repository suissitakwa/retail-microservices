apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: product-db-pvc
  namespace: retail-microservices-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-db
  namespace: retail-microservices-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-db
  template:
    metadata:
      labels:
        app: product-db
    spec:
      containers:
        - name: product-db
          image: postgres:14-alpine
          env:
            - name: POSTGRES_DB
              value: product_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: retail-ms-secrets
                  key: PRODUCT_DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: retail-ms-secrets
                  key: PRODUCT_DB_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: product-db-storage
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U $POSTGRES_USER -d product_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: product-db-storage
          persistentVolumeClaim:
            claimName: product-db-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: product-db
  namespace: retail-microservices-dev
spec:
  selector:
    app: product-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
