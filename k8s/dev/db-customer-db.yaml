apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: customer-db-pvc
  namespace: retail-microservices-dev
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: customer-db
  namespace: retail-microservices-dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: customer-db
  template:
    metadata:
      labels:
        app: customer-db
    spec:
      containers:
        - name: customer-db
          image: postgres:14-alpine
          env:
            - name: POSTGRES_DB
              value: customer_db
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: retail-ms-secrets
                  key: CUSTOMER_DB_USERNAME
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: retail-ms-secrets
                  key: CUSTOMER_DB_PASSWORD
            - name: PGDATA
              value: /var/lib/postgresql/data/pgdata
          ports:
            - containerPort: 5432
          volumeMounts:
            - name: customer-db-storage
              mountPath: /var/lib/postgresql/data
          readinessProbe:
            exec:
              command: ["sh", "-c", "pg_isready -U $POSTGRES_USER -d customer_db"]
            initialDelaySeconds: 10
            periodSeconds: 5
      volumes:
        - name: customer-db-storage
          persistentVolumeClaim:
            claimName: customer-db-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: customer-db
  namespace: retail-microservices-dev
spec:
  selector:
    app: customer-db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
